var firstItem = document.getElementById('one');         // Obtém o 1° item
if (firstItem.hasAttribute('class')) {                  // Se tiver o atributo class
  firstItem.removeAttribute('class');
}
